# Copy to ".env" (gitignored) for the guest-list importer ONLY.
# The service-role key bypasses Row Level Security — never commit it, never put
# it in js/ or anywhere the browser can see it.

SUPABASE_URL=https://tomfokjerpwoxgiqtdkd.supabase.co

# Supabase → Project Settings → API → service_role secret
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
