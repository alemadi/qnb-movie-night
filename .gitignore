# deps & build
node_modules/
package-lock.json

# local screenshots (regenerated by scripts/shoot.mjs)
scripts/preview_*.png

# secrets — service_role scripts must stay local & untracked
.env
*.local.*
scripts/*secret*
scripts/seed_local.*
