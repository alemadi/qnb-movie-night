{
  "name": "qnb-movie-checkin",
  "version": "1.0.0",
  "description": "QNB Movie Night check-in — static frontend (GitHub Pages) + Supabase backend. This package is only for the guest-list importer.",
  "type": "module",
  "private": true,
  "scripts": {
    "import": "node scripts/import.mjs",
    "import:dry": "node scripts/import.mjs --dry-run",
    "import:commit": "node scripts/import.mjs --commit"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "xlsx": "^0.18.5"
  }
}
